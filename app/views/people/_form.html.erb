<%= form_for(person) do |f| %>
  <% if person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(person.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dnis %>
    <%= f.number_field :dnis %>
  </div>

  <div class="field">
    <%= f.label :birth %>
    <%= f.date_select :birth %>
  </div>

  <div class="field">
    <%= f.label :promise_day %>
    <%= f.date_select :promise_day %>
  </div>

  <div class="field">
    <%= f.label :spr_person_id %>
    <%= f.text_field :spr_person_id %>
  </div>

  <div class="field">
    <%= f.label :parish_id %>
    <%= f.text_field :parish_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
